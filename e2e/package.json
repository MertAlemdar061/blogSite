{
  "name": "blog-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Tech Gündem blogu için Playwright uçtan uca (E2E) testleri",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  }
}
